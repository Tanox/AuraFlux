# npm 配置 - 使用全局包和避免本地 node_modules
registry=https://registry.npmmirror.com
auto-install-peers=true

# 使用全局依赖
package-lock=false
save-exact=false

# 缓存配置
cache=~/.npm/cache

# 脚本前缀配置
prefix=~/.npm/global
